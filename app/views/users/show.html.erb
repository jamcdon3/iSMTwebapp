<% if logged_in? and current_user[:admin] %>
<h1>
  Hello Administrator
</h1>
<% else %>
  <h1>
    Hello User
  </h1>
<% end %>
<hr>

<% if logged_in? and current_user[:admin] %>
<h2>Trial Runs</h2>
<%= render partial: 'shared/table', locals: { header: ['Run ID'],
                                              options: [{title: 'Show', url: "item_path(m)"},
                                                        {title: 'Edit', url: "edit_item_path(m)"},
                                                        {title: 'Destroy', url: "item_path(m)", method: :delete, data: { confirm: 'Are you sure?'} }],
                                              model: @items,
                                              row: [:id]} %>

<%= render 'users/admin' %>
<% end %>