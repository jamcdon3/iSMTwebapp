<% if is_logged_in? && is_admin? %>
  <h2 class="m-4 text-center">Create User</h2>
<% else %>
  <h2 class="m-4">Create your Account</h2>
<% end %>
<%= render partial: "form", locals: {url: users_path, method: :post, submitText: "Register"} %>